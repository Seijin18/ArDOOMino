# Arquivar este change (após T1 + T2 na placa)

1. Marcar todos os itens físicos em `tasks.md`.
2. Copiar deltas de `specs/` deste change para `openspec/specs/` (mesclar ADDED requirements).
3. Mover pasta para `openspec/changes/archive/validate-uart-hardware/` (ou usar `openspec archive` se CLI instalada).
4. Iniciar implementação de `integrate-ardoomino-game`.
